#!/bin/sh

# script prevents pushing to develop by mistake.
# master is part of the current deployment flow.

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "develop" ]; then
  echo "You can't push directly to develop branch"
  exit 1
fi
